import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#2F2F2F",
      main: "#1B1B1B",
    },
    secondary: {
      main: "#138c01",
    },
  },
});

export default theme;
