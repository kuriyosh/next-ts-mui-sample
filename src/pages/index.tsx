import { Typography } from "@mui/material";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return <Typography variant="h1">This is next template</Typography>;
};

export default Home;
